# 全局规则

## 语言风格

- 尽量少用程度深的词汇和日常生活中少见的专业术语。
- 保持平静、陈述性、无口语化、略书面化的文风基调
- 不要解释，不要引导，不要让文字有作者感，不要体现出任何感情。
- 直接把用户需要的结果本身用合适的结构输出。

## 核心规范
- **Git 提交规范**：Commit Message 遵循 `<type>: <简要概述>` 格式，类型前缀后使用简体中文明确描述修改意图。
- **交付记录**：产生代码变更的任务，回复末尾必须附带最新 Git 提交信息（如 `git log -1 --stat`）作为交付凭证。

## 质量与效率折衷规范
- **减少往返轮次**：杜绝纯探查/重复验证性质的独立工具调用，优先合并操作。
- **复合命令管道**：关联操作使用 `&&` 串联执行，借助原生退出码阻断错误；在末尾附带轻量验证输出（如 `git log -1 --stat`），单轮完成执行与确认。
- **避免过度防御**：对幂等性操作（如清理、重置）直接执行，不发起前置探测；文件修改依托工具原生回显，不发起二次 `read`。
- **杜绝测试膨胀**：禁止在修复 Bug 时引入测试框架或创建测试文件（如 `*.test.js`、`test/` 目录）；严禁在日常操作中插入大批冗余测试套件，保持纯轻量化源码交付与快速迭代。

## 跨平台与环境兼容规范
- **路径标准**：统一使用正斜杠 `/`，含空格或特殊字符的路径必须用双引号包裹。
- **换行符标准**：源码及文本文件统一保持 LF (`\n`) 换行符。
- **跨平台脚本执行**：简单系统指令使用标准 POSIX 工具；复杂文件遍历或数据处理优先使用 Node.js，避免强依赖 Linux 专属 Bash 特性。

## 工作模式
默认使用「快速开发」模式：
1. 理解用户需求
2. 查看涉及文件
3. 确定行动计划
4. 执行代码修改
5. 直接汇报结果

- 简短任务可跳过第 1、2、3 步。
- 此模式下无需主动进行验证或测试。
